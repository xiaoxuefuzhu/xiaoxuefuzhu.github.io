# 小学英语辅助 App — 使用说明（本地 App 模式）

外研版（新启航）一年级起点 · 一年级（上下册）+ 二年级（上下册）
单页 Web App：视频课 + 单词解析 + 点击朗读（预录音频）。

当前规模：**20 个单元、269 个单词、52 个视频**。

---

## 一、运行模式

本 App 采用**本地 App + 远程资源**模式：

- **App 本体**（`index.html`，约 55KB）放在本地，双击即用；
- **音视频资源**（52 个视频 + 269 个音频，共约 91MB）存储在 Gitee 仓库，运行时通过网络自动加载；
- 因此本地只需保留 `index.html` 一个文件，**不需要**把 media 目录放在本地。

> Gitee Pages 免费服务已于 2024 年对新用户下线，因此采用本方案替代。
> Gitee 仓库的 raw 文件地址可直接作为资源链接使用（已验证视频/音频均可正常播放）。

## 二、本地使用（电脑）

1. 确保电脑联网；
2. 双击 `index.html`，用 **Edge / Chrome** 打开；
3. 首页点「一年级」或「二年级」→ 选上下册 → 点单元进入课程；
4. 课程页横屏为左 70% 视频 + 右 30% 单词，竖屏为上 50% 视频 + 下 50% 单词；
5. 点击单词卡片即朗读发音；视频可拖动进度条、全屏。

## 三、手机 / 平板使用

**Android 手机/平板**：
1. 把 `index.html` 传到手机（微信文件传输助手、USB、网盘均可）；
2. 用手机自带的「文件管理」找到该文件，点击选择用浏览器打开（推荐 Edge、Chrome、系统浏览器）；
3. 联网即可使用。

**iPhone / iPad**：
1. 把 `index.html` 存到「文件」App 中；
2. 长按文件 →「共享」→「在 Safari 中打开」（或拷贝到 Safari 地址栏）；
3. iOS 对本地 HTML 支持有限，如遇问题建议用电脑或 Android。

**微信内使用**：微信无法直接打开本地 HTML 文件。建议在微信里把文件发给「文件传输助手」，然后用其他应用打开（选择浏览器）。

## 四、Gitee 仓库说明

仓库地址：https://gitee.com/zhuxinru_ts/english-helper

仓库中已上传：
- `index.html`（App 本体，与本地一致）
- `media/video/`（52 个课程视频）
- `media/audio/`（269 个单词音频）
- 4 个单词源 txt、部署说明等

资源通过以下格式的 raw 地址加载：
```
https://gitee.com/zhuxinru_ts/english-helper/raw/master/media/video/u1_chant.mp4
https://gitee.com/zhuxinru_ts/english-helper/raw/master/media/audio/u1_hello.mp3
```

`index.html` 顶部的 `RESOURCE_BASE` 变量即为此地址前缀。如更换仓库或用户名，修改此变量即可。

## 五、后续更新内容

**更新单词/视频后**：
1. 本地修改单词源 txt 或替换视频文件；
2. 重新运行构建脚本（`tools/build_words.py` → `gen_audio.py` → `setup_videos.py` → `inject_data.py`）；
3. 把更新后的 `index.html` 和 `media/` 推送到 Gitee：
   ```bash
   cd /d D:\小学英语辅助
   git add .
   git commit -m "update content"
   git push
   ```
4. 推送完成后，本地 `index.html` 会自动加载 Gitee 上的新资源（无需更新本地文件，除非改了页面逻辑）。

**新增年级/册**：参考 `tools/build_words.py` 中的 `SOURCES` 配置，登记新单词源文件和起始单元 id，然后按上述流程构建并推送。

## 六、已知事项

1. **必须联网**：音视频从 Gitee 远程加载，无网络时无法播放视频和发音（页面本身可打开，但资源加载失败）；
2. **视频水印**：部分视频画面带「三学编程 bilibili」水印，源自下载源，介意需换源；
3. **目录名笔误**：本地源目录 `英文-二级年上册` 应为 `英文-二年级上册`（不影响功能，介意可自行重命名后同步修改 `tools/setup_videos.py`）；
4. **Logo**：当前为临时占位（蓝色「英」字），确认最终 logo 后替换导航栏 40×40 与首页 108px 两处即可；
5. **Gitee raw 限流**：个人/家庭使用量小，不会触发限流；如大规模分享（数千人同时访问），可能被 Gitee 限流，届时建议迁移到专业静态托管。

## 七、风险与注意事项

- **视频版权**：课程视频为教材配套资源（网络下载），仓库公开后任何人可访问 raw 地址下载，存在版权风险。请自行评估；如在意，可将仓库设为私有（但私有仓库的 raw 地址需要登录才能访问，本地 App 将无法加载资源——因此当前必须保持公开）。
- **Gitee 账号安全**：仓库已公开，请勿在仓库中存放密码、私人令牌等敏感信息。
