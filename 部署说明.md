# 小学英语辅助 App — 使用与部署说明

外研版（新启航）一年级起点 · 一年级（上下册）+ 二年级（上下册）
单页 Web App：视频课 + 单词解析 + 点击朗读（预录音频）。

当前规模：**20 个单元、269 个单词、52 个视频**（一年级上 5 单元 / 一年级下 5 单元 / 二年级上 5 单元 / 二年级下 5 单元）。

---

## 一、项目文件

```
D:\小学英语辅助\
├── index.html                App 本体（单页，含全部数据与逻辑）
├── U1-U5单词.txt              一年级上册单词源（5 单元 71 词）
├── 一年级下.txt               一年级下册单词源（5 单元 81 词）
├── 二年级上.txt               二年级上册单词源（5 单元 61 词）
├── 二年级下.txt               二年级下册单词源（5 单元 56 词）
├── 英文-一年级上册\            一年级上册视频源（12 个）
├── 英文-一年级下册\            一年级下册视频源（20 个）
├── 英文-二级年上册\            二年级上册视频源（10 个，目录名含笔误）
├── 英文-二年级下册\            二年级下册视频源（10 个）
├── media\
│   ├── video\                52 个课程视频（u1_… … u20_…）
│   └── audio\                269 个单词音频（u1_hello.mp3 … u20_wait.mp3）
├── tools\                    构建脚本（更新数据/音频时使用，可不随 App 上传）
│   ├── build_words.py        解析 4 册单词清单 → words_data.json
│   ├── gen_audio.py          edge-tts 增量生成单词 mp3
│   ├── setup_videos.py       从 4 个视频源目录复制并重命名视频
│   ├── inject_data.py        把 words_data.json 注入 index.html
│   ├── words_data.json       结构化单词数据（中间产物）
│   ├── rename_ids.py         单元 id 重排时重命名 media 资源（已用完）
│   └── verify_shots.py       页面截图自检脚本
└── 部署说明.md               本文件
```

## 二、本地使用

直接双击 `D:\小学英语辅助\index.html`，用 **Edge / Chrome / Safari** 打开即可。
- 首页点「一年级」或「二年级」进入单元列表（上下册分组），再点单元卡片进入课程；
- 课程页横屏为左 70% 视频 + 右 30% 单词，竖屏为上 50% 视频 + 下 50% 单词；
- 视频标签随册不同：一年级上（chant/read，U2/U3 另有拓展）、一年级下（chant/read/拓展词汇/拓展对话）、二年级（chant/read 或 chant/主题故事）；
- 点击单词卡片即朗读发音（预录音频，微信内也能发声）。

## 三、部署到 Gitee，生成公开网址

> 前提：Gitee 账号已完成**实名认证**（未实名无法开通 Pages）。

### 第 1 步：安装 Git（如已安装可跳过）
到 https://git-scm.com/download/win 下载安装，一路下一步即可。

### 第 2 步：创建仓库
1. 登录 https://gitee.com ，点右上角「+」→「新建仓库」；
2. 仓库名称填英文（如 `english-helper`），**必须选「公开」**（私有仓库无法开通 Pages）；
3. 「是否初始化仓库」选「不初始化」；
4. 点「创建」。

### 第 3 步：上传项目文件
打开「命令提示符 / PowerShell」，逐行执行（把 `你的用户名` 换成你的 Gitee 用户名）：

```bash
cd /d D:\小学英语辅助
git init
git add .
git commit -m "init"
git remote add origin https://gitee.com/你的用户名/english-helper.git
git push -u origin master
```

第一次推送会要求输入 Gitee 账号密码（密码为 Gitee 登录密码；若设置了私人令牌则用令牌）。

> 不想用命令行的替代方式：进入仓库页面 →「文件」→「上传文件」，先在网页里依次新建
> `media/video`、`media/audio` 目录并上传对应文件，再在根目录上传 `index.html`。
> 注意：页面引用路径区分大小写，目录名必须与上面完全一致。

### 第 4 步：开通 Gitee Pages
1. 进入仓库 → 顶部菜单「服务」→「Gitee Pages」；
2. 部署分支选 `master`，部署目录填 `/`；
3. 点「启动」，等待审核通过（免费服务通常需数分钟到数小时）。

### 第 5 步：访问
部署成功后，网址为：

```
https://你的用户名.gitee.io/english-helper/
```

把链接发到微信即可使用；建议家长在微信内点右上角「…」→「在浏览器打开」，播放更稳定。

## 四、已知缺口（需要补充资料）

1. **视频水印**：部分视频画面带「三学编程 bilibili」水印，源自下载源，介意需换源；
2. **目录名笔误**：`英文-二级年上册` 应为 `英文-二年级上册`（脚本按当前实际目录名工作，不影响功能，介意可自行重命名后同步修改 `setup_videos.py`）；
3. **Logo**：当前为临时占位，见"风险与注意事项"第 3 条。

## 五、后续扩展（新年级 / 补单元）

> 当前单元 id 体系：G1上 1-5 / G1下 6-10 / G2上 11-15 / G2下 16-20；新年级按序接续。

1. **单词数据**：新单词按相同格式（`### 单元标题` + `* 单词 (释义)`，可带 emoji 或 ** 加粗，括号支持全角/半角）整理成 txt，放入项目根目录；
2. 在 `tools/build_words.py` 的 `SOURCES` 列表登记（文件、年级、册、起始单元 id），运行生成 `words_data.json`；
3. **单词音频**：运行 `python tools/gen_audio.py`（增量生成，需 `pip install edge-tts`）；
4. **视频**：在 `tools/setup_videos.py` 的 `SOURCES` 登记新目录与文件名映射，运行复制；再在 `index.html` 顶部 `VIDEO_MAP` 登记；
5. **页面配置**：`index.html` 中 `GRADE_BOOKS` 登记单元 id、`AVAILABLE_GRADES` 加入年级号；
6. 运行 `python tools/inject_data.py` 把新数据写回 `index.html`（或联系我帮你处理）。

## 六、风险与注意事项

- **视频版权**：课程视频为教材配套资源（网络下载），仓库公开后任何人可访问，存在版权风险。请自行评估；如在意，可不上传视频或仅限家庭内部使用。
- **实名认证**：开通 Pages 需提交身份证信息，按 Gitee 要求操作即可。
- **Logo**：当前为临时占位（蓝色「英」字），确认最终 logo 后替换导航栏 40×40 与首页 108px 两处即可。
