# 小学英语辅助 App — 使用说明（本地 App 模式）

外研版（新启航）一年级起点 · 一年级（上下册）+ 二年级（上下册）
单页 Web App：视频课 + 单词解析 + 点击朗读（预录音频）。

当前规模：**20 个单元、269 个单词、52 个视频**。

---

## 一、运行模式

本 App 采用**本地 App + 远程资源**模式：

- **App 本体**（`index.html`，约 55KB）放在本地，双击即用；
- **音视频资源**（52 个视频 + 269 个音频，共约 91MB）存储在 Gitee 仓库，运行时通过网络自动加载；
- 因此本地只需保留 `index.html` 一个文件，**不需要**把 media 目录放在本地。

> Gitee Pages 免费服务已于 2024 年对新用户下线，因此采用本方案替代。
> Gitee 仓库的 raw 文件地址可直接作为资源链接使用（已验证视频/音频均可正常播放）。

## 二、本地使用（电脑）

1. 确保电脑联网；
2. 双击 `index.html`，用 **Edge / Chrome** 打开；
3. 首页点「一年级」或「二年级」→ 选上下册 → 点单元进入课程；
4. 课程页横屏为左 70% 视频 + 右 30% 单词，竖屏为上 50% 视频 + 下 50% 单词；
5. 点击单词卡片即朗读发音；视频可拖动进度条、全屏。

## 三、手机 / 平板使用

**Android 手机/平板**：
1. 把 `index.html` 传到手机（微信文件传输助手、USB、网盘均可）；
2. 用手机自带的「文件管理」找到该文件，点击选择用浏览器打开（推荐 Edge、Chrome、系统浏览器）；
3. 联网即可使用。

**iPhone / iPad**：
1. 把 `index.html` 存到「文件」App 中；
2. 长按文件 →「共享」→「在 Safari 中打开」（或拷贝到 Safari 地址栏）；
3. iOS 对本地 HTML 支持有限，如遇问题建议用电脑或 Android。

**微信内使用**：微信无法直接打开本地 HTML 文件。建议在微信里把文件发给「文件传输助手」，然后用其他应用打开（选择浏览器）。

## 四、Gitee 仓库说明

仓库地址：https://gitee.com/zhuxinru_ts/english-helper

仓库中已上传：
- `index.html`（App 本体，与本地一致）
- `media/video/`（52 个课程视频）
- `media/audio/`（269 个单词音频）
- 4 个单词源 txt、部署说明等

资源通过以下格式的 raw 地址加载：
```
https://gitee.com/zhuxinru_ts/english-helper/raw/master/media/video/u1_chant.mp4
https://gitee.com/zhuxinru_ts/english-helper/raw/master/media/audio/u1_hello.mp3
```

`index.html` 顶部的 `RESOURCE_BASE` 变量即为此地址前缀。如更换仓库或用户名，修改此变量即可。

## 五、后续更新内容

**更新单词/视频后**：
1. 本地修改单词源 txt 或替换视频文件；
2. 重新运行构建脚本（`tools/build_words.py` → `gen_audio.py` → `setup_videos.py` → `inject_data.py`）；
3. 把更新后的 `index.html` 和 `media/` 推送到 Gitee：
   ```bash
   cd /d D:\小学英语辅助
   git add .
   git commit -m "update content"
   git push
   ```
4. 推送完成后，本地 `index.html` 会自动加载 Gitee 上的新资源（无需更新本地文件，除非改了页面逻辑）。

**新增年级/册**：参考 `tools/build_words.py` 中的 `SOURCES` 配置，登记新单词源文件和起始单元 id，然后按上述流程构建并推送。

## 六、已知事项

1. **必须联网**：音视频从 Gitee 远程加载，无网络时无法播放视频和发音（页面本身可打开，但资源加载失败）；
2. **视频水印**：部分视频画面带「三学编程 bilibili」水印，源自下载源，介意需换源；
3. **目录名笔误**：本地源目录 `英文-二级年上册` 应为 `英文-二年级上册`（不影响功能，介意可自行重命名后同步修改 `tools/setup_videos.py`）；
4. **Logo**：当前为临时占位（蓝色「英」字），确认最终 logo 后替换导航栏 40×40 与首页 108px 两处即可；
5. **Gitee raw 限流**：个人/家庭使用量小，不会触发限流；如大规模分享（数千人同时访问），可能被 Gitee 限流，届时建议迁移到专业静态托管。

## 七、部署到腾讯云 COS（推荐网址访问方式）

将 `index.html` 部署到腾讯云对象存储（COS），通过网址访问，国内速度快，适合微信分享。音视频仍从 Gitee 加载，COS 只托管一个 55KB 的 HTML 文件。

### 费用说明

- **新用户免费**：50GB 标准存储容量，免费 6 个月（腾讯云官方政策）；
- **6 个月后**：按量付费，本场景费用极低——存储仅 55KB（约 0.000006 元/月），每次访问下载 55KB，每月 1000 次访问约 1.6GB 流量（约 0.8 元/月）；
- **无需备案**：使用 COS 默认域名（`cos-website` 域名）访问，不需要 ICP 备案；
- **账户需保持余额**：按量付费模式下，腾讯云账户需有少量余额（建议充 10 元可用一年以上）。

### 部署步骤

**第 1 步：注册腾讯云账号并实名认证**
- 访问 https://cloud.tencent.com/ ，微信扫码注册；
- 右上角头像 → 账号信息 → 实名认证 → 个人认证（需身份证，几分钟完成）。

**第 2 步：开通对象存储 COS**
- 登录后访问 https://console.cloud.tencent.com/cos ；
- 点击「开通 COS 服务」（免费开通，开通即享新用户 50GB 免费额度）。

**第 3 步：创建存储桶**
- COS 控制台左侧「存储桶列表」→「创建存储桶」；
- **名称**：填英文，如 `english-helper`（会生成唯一名称 english-helper-你的APPID）；
- **地域**：选离你最近的（如广州、上海、北京）；
- **访问权限**：必须选「**公有读私有写**」（否则无法公开访问）；
- 其他默认，点击「创建」。

**第 4 步：上传 index.html**
- 进入刚创建的存储桶 → 左侧「文件列表」→「上传文件」；
- 选择 `D:\小学英语辅助\index.html`，点击「确定上传」；
- 上传完成后，文件列表中会出现 `index.html`。

**第 5 步：开启静态网站托管**
- 存储桶左侧菜单：「基础配置」→「静态网站」；
- 点击「编辑」；
- **当前状态**：选「开启」；
- **索引文档**：填 `index.html`；
- **错误文档**：可选填 `index.html`（单页应用推荐，避免刷新 404）；
- 其他默认，点击「保存」。

**第 6 步：获取访问网址并测试**
- 静态网站配置页面会显示「访问节点」（也叫访问域名）；
- 格式：`english-helper-1234567890.cos-website.ap-guangzhou.myqcloud.com`；
- 复制该网址，用浏览器打开，测试首页、年级页、课程页、视频播放、单词发音是否正常；
- 将网址发到微信，即可在微信内打开使用。

### 注意事项

- **音视频从 Gitee 加载**：COS 上只需上传 `index.html`，不需要上传 media 目录（index.html 中的 `RESOURCE_BASE` 已指向 Gitee raw 地址）；
- **更新内容**：以后修改 index.html 后，在 COS 文件列表中删除旧的 index.html，重新上传新的即可（覆盖上传）；音视频更新推送到 Gitee 后自动生效，无需操作 COS；
- **自定义域名**：如想用自己的域名（如 english.example.com），需先完成 ICP 备案，再在 COS「域名与传输管理」→「自定义源站域名」中绑定；
- **防盗链**：如担心网址被大量盗用产生流量费，可在 COS「安全管理」→「防盗链」中设置 Referer 白名单（仅允许微信、浏览器等来源访问）；
- **COS 默认域名 vs 对象域名**：静态网站必须用 `cos-website` 域名访问（第 6 步给出的），不要用普通的 `cos.ap-region.myqcloud.com` 对象域名（那个会直接下载文件而不是渲染网页）。

## 八、风险与注意事项

- **视频版权**：课程视频为教材配套资源（网络下载），仓库公开后任何人可访问 raw 地址下载，存在版权风险。请自行评估；如在意，可将仓库设为私有（但私有仓库的 raw 地址需要登录才能访问，本地 App 将无法加载资源——因此当前必须保持公开）。
- **Gitee 账号安全**：仓库已公开，请勿在仓库中存放密码、私人令牌等敏感信息。

## 九、打包成 Android App（推荐手机使用方式）

将 `index.html` 打包成 Android APK 后，手机上安装即可像普通 App 一样点开使用，无需每次找文件。

### 打包项目文件

`app_package/` 目录已准备好打包所需的全部文件：

```
app_package/
├── index.html      App 本体（与根目录一致）
├── manifest.json   HBuilderX 应用配置（应用名、版本、权限、横竖屏）
└── icon.png        应用图标（512×512，蓝色"英"字）
```

### 打包步骤（HBuilderX 云打包，免费）

**第 1 步：下载安装 HBuilderX**
- 访问 https://www.dcloud.io/hbuilderx.html
- 下载「**App 开发版**」（标准版不含云打包功能）
- 安装后打开，注册/登录 DCloud 账号（免费）

**第 2 步：导入项目**
- HBuilderX 菜单：文件 → 导入 → 从本地目录导入
- 选择 `D:\小学英语辅助\app_package` 目录
- 左侧文件树会出现 `app_package` 项目

**第 3 步：确认应用配置**
- 双击项目中的 `manifest.json`，打开可视化配置界面
- 「基本配置」：应用名称「小学英语辅助」，版本号 1.0.0（已预设，可直接用）
- 「图标配置」：点击「选择」，浏览到 `app_package/icon.png`，点击「自动生成所有图标」
- 「模块配置」：不需要勾选任何模块（保持默认）
- 「权限配置」：确认已勾选 `INTERNET`（网络访问，已预设）
- 按 Ctrl+S 保存

**第 4 步：云打包**
- 菜单：发行 → 原生App-云打包
- 打包平台：勾选「**Android**」（iOS 需苹果开发者账号 $99/年，个人使用不建议）
- 证书：选择「**使用公共测试证书**」（DCloud 提供的免费证书，个人使用足够；如需上架应用商店才需自有证书）
- 打包类型：「正式包」
- 点击「打包」

**第 5 步：等待打包完成**
- 云打包通常需要 1-5 分钟（高峰期可能排队）
- 打包过程中 HBuilderX 控制台会显示进度
- 完成后会弹出下载链接，或在控制台点击下载 APK

**第 6 步：安装到手机**
- 把下载的 APK 文件传到手机（微信文件传输助手、USB、网盘均可）
- 手机上点击 APK 文件安装
- 首次安装可能提示「未知来源应用」，在设置中允许即可
- 安装后桌面出现「小学英语辅助」图标，点开即用

### 注意事项

- **必须联网**：App 内音视频从 Gitee 远程加载，无网络时无法播放；
- **Android 版本**：支持 Android 5.0 及以上（minSdkVersion=21），覆盖绝大多数设备；
- **iOS**：当前方案不支持 iOS（需苹果开发者账号）。iPhone/iPad 用户建议用电脑或 Android 设备，或参考第三章的本地文件方式；
- **更新内容后**：重新构建 index.html → 复制到 `app_package/` → 重新云打包 → 重新安装 APK（音视频更新推送到 Gitee 后，已安装的 App 自动加载新资源，无需重装）；
- **公共测试证书**：生成的 APK 可正常安装使用，但不能上架 Google Play / 国内应用商店（个人家庭使用无影响）；
- **DCloud 实名认证**：首次云打包可能要求完成 DCloud 实名认证，按提示操作即可（与 Gitee 账号无关）。
